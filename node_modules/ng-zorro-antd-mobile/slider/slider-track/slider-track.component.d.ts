import { ElementRef, OnChanges } from '@angular/core';
import { DomSanitizer } from '@angular/platform-browser';
export declare class SliderTrack implements OnChanges {
    private _elf;
    private _sanitizer;
    prefixCls: string;
    elStyle: object;
    private _className;
    private _included;
    private _offset;
    private _length;
    private _style;
    className: object;
    included: boolean;
    offset: number;
    length: number;
    style: object;
    constructor(_elf: ElementRef, _sanitizer: DomSanitizer);
    ngOnChanges(): void;
}
