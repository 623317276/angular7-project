import { EventEmitter } from '@angular/core';
export interface OnChangeEvent {
    selectedIndex: number;
    value: string;
}
export declare class SegmentedControlComponent {
    prefixCls: string;
    tintColor: string;
    disabled: boolean;
    selectedIndex: number;
    values: Array<string>[];
    onChange: EventEmitter<OnChangeEvent>;
    role: string;
    amSegment: boolean;
    readonly amDisabled: boolean;
    constructor();
    onClick(index: number, value: string): void;
}
