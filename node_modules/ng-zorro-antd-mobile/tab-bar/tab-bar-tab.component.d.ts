import { AfterContentInit, EventEmitter, TemplateRef } from '@angular/core';
import { TabPane } from '../tabs/tab-pane.component';
export interface OnPressEvent {
    title: string;
    key: string;
}
export declare class TabBarTab implements AfterContentInit {
    tabPane: TabPane;
    prefixCls: string;
    selected: boolean;
    tintColor: string;
    unselectedTintColor: string;
    key: string;
    title: string;
    dot: boolean;
    badge: number | string;
    icon: string | TemplateRef<void>;
    selectedIcon: string | TemplateRef<void>;
    onPress: EventEmitter<OnPressEvent>;
    tabBarTab: boolean;
    constructor(tabPane: TabPane);
    isTemplateRef(value: any): boolean;
    ngAfterContentInit(): void;
}
