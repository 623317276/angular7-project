import { Models } from '../date/DataTypes';
export declare const mergeDateTime: (date?: Date, time?: Date) => Date;
export declare const formatDate: (date: Date, format: string, locale?: Models.Locale) => string;
