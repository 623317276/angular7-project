import { OnInit, EventEmitter, OnChanges, SimpleChanges, AfterViewInit } from '@angular/core';
import { DatePickerComponent } from '../date-picker/date-picker.component';
export declare class DatePickerViewComponent extends DatePickerComponent implements OnInit, AfterViewInit, OnChanges {
    mode: string;
    minDate: Date;
    maxDate: Date;
    value: Date;
    disabled: boolean;
    indicatorStyle: object;
    locale: any;
    onValueChange: EventEmitter<any>;
    amPicker: boolean;
    init(): void;
    reloadPicker(): void;
    ngOnInit(): void;
    ngAfterViewInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
}
