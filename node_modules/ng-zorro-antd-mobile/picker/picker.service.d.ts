import { PickerCallBack, PickerOptions } from './picker-options.provider';
import { PopupService } from '../core/core.module';
export declare class Picker extends PopupService {
    static defaultOptions: PickerOptions;
    static showPicker(config?: PickerOptions, confirmCallback?: PickerCallBack, cancelCallback?: PickerCallBack): void;
    static hidePicker(): void;
}
