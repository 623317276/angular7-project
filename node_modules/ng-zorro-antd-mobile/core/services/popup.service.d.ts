import { Injector, ViewContainerRef } from '@angular/core';
import { Overlay, OverlayRef } from '@angular/cdk/overlay';
export declare class PopupService {
    _overlay: Overlay;
    _viewContainerRef: ViewContainerRef;
    static viewContainerRef: ViewContainerRef;
    static overlay: Overlay;
    static overlayRef: OverlayRef;
    constructor(_overlay: Overlay, _viewContainerRef: ViewContainerRef);
    static showPopup(component: any, childInjector?: Injector): void;
    static hidePopup(): void;
}
